# LINE webhook server
fastapi>=0.111.0
uvicorn>=0.29.0
line-bot-sdk>=3.11.0
python-dotenv>=1.0.0

# LLM clients
# boto3 is lazy-imported inside BedrockClaudeClient (only needed when USE_BEDROCK=true).
# Install separately to enable Bedrock:
#   pip install boto3>=1.34.0
anthropic>=0.25.0
google-genai>=1.0.0

# GCS — lazy-imported, only needed when GCS_PROFILES_BUCKET is set
google-cloud-storage>=2.14.0

# Dev / test
pytest>=8.0.0
httpx>=0.27.0
